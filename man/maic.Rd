% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maic.R
\name{maic}
\alias{maic}
\title{Function to do MAIC}
\usage{
maic(
  AB_IPD,
  AC_aggregate,
  adjust_all = TRUE,
  type = c("rescaled_weights", "weights")
)
}
\arguments{
\item{AB_IPD}{A data.frame of IPD from the AB trial. Generated by `gen_data()`.}

\item{AC_aggregate}{A data.frame of aggregate data from the AC trial. Generated
by `gen_data()`}

\item{adjust_all}{A logical value indicating whether to adjust for all effect
modifiers. Default is TRUE. If TRUE we adjust by both effect modifiers X1 and
X2, if FALSE we only adjust by X1.}

\item{type}{A character string specifying the type of weights to use. 
Options are "rescaled_weights" or "weights". "rescaled_weights" 
rescales the weights so they sum to the original sample size, while "weights" 
uses the raw propensity score weights. Default is "rescaled_weights".}
}
\value{
A data.frame of maic results, consisting of maic_log_OR, maic_ESS,
maic_se, maic_ci_lower, and maic_ci_upper.
}
\description{
This function performs matching-adjusted indirect comparison (MAIC).
}
\examples{
# generate data
test_data <- gen_data()

# do maic
maic(test_data$AB_IPD, test_data$AC_aggregate)

}
